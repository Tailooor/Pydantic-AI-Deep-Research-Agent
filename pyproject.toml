[project]
name = "pydantic-ai-deep-research-agent"
version = "0.1.0"
description = "Simple Pydantic AI agent with an OpenRouter free model and Gradio UI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ddgs>=9.6.1",
    "gradio>=6.13.0",
    "pydantic-ai>=1.84.1",
]
